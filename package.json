{
  "name": "triptravel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client start",
    "build": "npm --prefix client run build",
    "start": "node server/index.js"
  },
  "engines": {
    "node": "18.x"
  }
}
